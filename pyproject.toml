[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "bizplots"
description = "Personal plotting utilities"
readme = "README.rst"
requires-python = ">=3.8"
version = "0.1.0"
